/**
 * Bootstrap Infrastructure Module
 *
 * Central export point for all bootstrap and lifecycle management services
 *
 * Organized into:
 * - Core: Application bootstrap, server startup, database initialization
 * - Features: Extended bootstrap features (reserved for future)
 * - Utils: Bootstrap utilities and types (reserved for future)
 */

// Core bootstrap services
export * from './core';

// Features (currently empty, reserved for future)
// export * from './features';

// Utilities (currently empty, reserved for future)
// export * from './utils';

