services:
  - type: web
    name: ordira-backend
    env: node
    plan: starter
    buildCommand: npm install && cd packages/backend && npm install --include=optional --legacy-peer-deps && npm run build
    startCommand: cd packages/backend && npm start
    envVars:
      - key: NODE_VERSION
        value: 22.11.0